[build]
builder = "NIXPACKS"
buildCommand = "cd ../../ && pnpm install --frozen-lockfile && pnpm --filter shared build && pnpm --filter frontend build"

[deploy]
startCommand = "npx serve dist --listen $PORT"
