[build]
builder = "NIXPACKS"
buildCommand = "cd ../../ && pnpm install && pnpm --filter shared build && pnpm --filter backend build"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
