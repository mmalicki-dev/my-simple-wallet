[build]
builder = "NIXPACKS"
buildCommand = "cd ../../ && pnpm install --frozen-lockfile && pnpm build:backend"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
