[build]
builder = "NIXPACKS"
buildCommand = "cd ../../ && pnpm install --frozen-lockfile && pnpm --filter shared build && pnpm --filter backend build"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
